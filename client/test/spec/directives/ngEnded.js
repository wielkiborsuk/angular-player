'use strict';

describe('Directive: ngEnded', function () {
  beforeEach(module('clientApp'));

  var element;

  // it('should make hidden element visible', inject(function ($rootScope, $compile) {
  //   element = angular.element('<ng-ended></ng-ended>');
  //   element = $compile(element)($rootScope);
  //   expect(element.text()).toBe('this is the ngEnded directive');
  // }));
});
